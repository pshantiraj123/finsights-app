apiVersion: v1
kind: Service
metadata:
  name: finsights-service
spec:
  type: NodePort
  selector:
    app: finsights
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
